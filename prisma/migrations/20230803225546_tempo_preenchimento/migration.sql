BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Formulario] ALTER COLUMN [termo] NVARCHAR(1000) NULL;
ALTER TABLE [dbo].[Formulario] ADD [duracao] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
